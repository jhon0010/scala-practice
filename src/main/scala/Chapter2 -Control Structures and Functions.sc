/**
  * 2.1 conditional expressions
  *
  * in Scala, every expression is supposed to have some value.
*/

val x = 0
val s = if (x > 0) 1 else -1

if (x > 0) "positive" else -1  // returns any
if (x > 0) 1 // if the condition does not apply the vale is Uint = () is like void in java

